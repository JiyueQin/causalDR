% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_estimates}
\alias{plot_estimates}
\title{Plot estimated causal estimands over time}
\usage{
plot_estimates(
  fit,
  estimand = "all_CIF",
  CI = T,
  xlab = "Time",
  ylab = "Value",
  time_cut = NULL,
  title = NULL,
  point_size = 1,
  pointrange_size = 0.1
)
}
\arguments{
\item{fit}{A fitted object returned by \code{causalAIPCW()} (or a compatible object)
containing \code{fit$est} (point estimates) and optionally \code{fit$est_se}
(estimates with confidence intervals).}

\item{estimand}{Character string specifying which family of estimands to plot.
Supported values include:
\itemize{
\item \code{"all_CIF"} (default): cumulative incidence functions for all event types
(and available contrasts).
\item \code{"all_Lambda"}: cumulative hazard functions for all transitions.
}}

\item{CI}{Logical; if \code{TRUE} (default), plots pointwise confidence intervals using
\code{fit$est_se}. If \code{FALSE}, plots point estimates using \code{fit$est} only.}

\item{xlab}{Character x-axis label. Default is \code{"Time"}.}

\item{ylab}{Character y-axis label. Default is \code{"Value"}.}

\item{time_cut}{Optional numeric value. If provided, restricts the plot to time points
\code{time <= time_cut}.}

\item{title}{Optional character string giving the plot title.}

\item{point_size}{Numeric point size passed to \code{ggplot2::geom_point()}.}

\item{pointrange_size}{Numeric line width for confidence interval bars produced by
\code{ggplot2::geom_pointrange()} when \code{CI = TRUE}.}
}
\value{
A \pkg{ggplot2} object.
}
\description{
Produces publication-ready plots of time-varying causal estimands (e.g., cumulative
incidence functions or cumulative hazards) from a fitted object returned by
\code{causalAIPCW()}.
}
\details{
The function supports plotting point estimates alone or point estimates with
pointwise confidence intervals, and automatically facets plots by estimand.
}
\examples{
\dontrun{
## Simulate data and fit the model
dat <- sim_data(n = 500)
fit <- causalAIPCW(
  data = dat,
  covars = c("Z1", "Z2"),
  estimand = "all",
  time1_interest = c(1, 2),
  n_boot = 100,
  seed = 1
)

## Plot CIFs with confidence intervals
plot_estimates(fit, estimand = "all_CIF", CI = TRUE)}
}
\seealso{
\code{\link{causalAIPCW}}, \code{\link{sim_data}}
}
